# F4P ISM Auto-Fetcher — Python Dependencies
# Install: pip install -r requirements.txt

requests>=2.31.0          # FRED API HTTP calls

# Optional — only needed if using Google Sheets push
# gspread>=6.0.0
# google-auth>=2.27.0
